let num = [];
const terminal = require('readline-sync');

/*console.log('Digite um número');
num[0] = parseInt(terminal.prompt());

console.log('Digite um número');
num[1] = parseInt(terminal.prompt());

console.log('Digite um número');
num[2] = parseInt(terminal.prompt());

let maior = num[0];

for(let i = 0; i < num.length; i++) {
    if(maior < num[i]) {
        maior = num[i];
    }
}

console.log(`O maior número é ${maior}`);

console.log('Digite um número');
num[0] = parseInt(terminal.prompt());

console.log('Digite um número');
num[1] = parseInt(terminal.prompt());

console.log('Digite um número');
num[2] = parseInt(terminal.prompt());

let menor = num[0];

for(let i = 0; i < num.length; i++) {
    if(menor > num[i]) {
        menor = num[i];
    }
}

console.log(`O menor número é ${menor}`);


function continha(num) {
    if(num > 1) {
        return num = num * continha(num - 1)
    } else {
        return num = 1;
    }
}

console.log(`${continha(6)}`);*/
